# Workspace de pilotage PASS — variables optionnelles.
# AUCUN secret ne doit être committé. Le jeton de la passerelle se saisit
# dans l'outil (réglages « Passerelle ANSUT »), pas ici.

# URL par défaut de la fonction edge de notification du Hub ANSUT (ansut-notify).
# Optionnel : sinon l'outil propose déjà cette valeur par défaut.
VITE_ANSUT_NOTIFY_URL=https://lpkfwxisranmetbtgxrv.supabase.co/functions/v1/ansut-notify
